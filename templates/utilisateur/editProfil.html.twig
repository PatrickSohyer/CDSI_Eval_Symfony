{% extends 'base.html.twig' %}

{% block title %}Inscription!
{% endblock %}

{% block navbar %}{% endblock %}

{% block body %}

	<div class="col-5 text-center mx-auto">
    <h1>Modifier utilisateur</h1>
		{{ form_start(formEditUser, {'attr' : {'novalidate' : 'novalidate' }}) }}

		{{ form_row(formEditUser.nom)}}
		{{ form_row(formEditUser.prenom)}}
		{{ form_row(formEditUser.roles)}}
		{{ form_row(formEditUser.email)}}
        {{ form_row(formEditUser.formation)}}
		{{ form_row(formEditUser.submit)}}

		{{ form_end(formEditUser) }}

	</div>
{% endblock %}
